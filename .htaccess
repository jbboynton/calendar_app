# Rewrites the URL to include a variable that can later be parsed.  This
# provides the basis of the application's routing scheme.  See
# /app/core/app.php for further information about the parseUrl() method.

# Options -MultiViews
# RewriteEngine On

# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteCond %{REQUEST_FILENAME} !-f

# RewriteRule ^(.+)$ index.php?url=$1 [QSA,L]
